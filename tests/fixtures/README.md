# Test Fixtures

Small golden fixtures live here when they are shared by multiple crates.
Generated media fixtures should stay generated inside tests unless a test needs
a stable byte-for-byte parser input.
