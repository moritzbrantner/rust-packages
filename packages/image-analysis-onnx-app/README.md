# image-analysis-onnx-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `image-analysis-onnx`.

Run the server:

```bash
cargo run -p image-analysis-onnx-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/image-analysis-onnx-app dev
```
