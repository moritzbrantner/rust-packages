# geo-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `geo-data`.

Run the server:

```bash
cargo run -p geo-data-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/geo-data-app dev
```
