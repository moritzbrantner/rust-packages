# audio-generation-tts-app

React, TypeScript, TailwindCSS, Bun, and Vite frontend for
`audio-generation-tts`.

```bash
cargo run -p audio-generation-tts-server -- --addr 127.0.0.1:3000
bun run --cwd packages/audio-generation-tts-app dev
```
