# math-signal-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `math-signal-core`.

Run the server:

```bash
cargo run -p math-signal-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/math-signal-core-app dev
```
