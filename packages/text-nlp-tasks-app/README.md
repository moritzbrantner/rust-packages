# text-nlp-tasks-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `text-nlp-tasks`.

Run the server:

```bash
cargo run -p text-nlp-tasks-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/text-nlp-tasks-app dev
```
