# geo-io-geojson-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `geo-io-geojson`.

Run the server:

```bash
cargo run -p geo-io-geojson-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/geo-io-geojson-app dev
```
