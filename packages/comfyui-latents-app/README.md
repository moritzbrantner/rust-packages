# comfyui-latents-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `comfyui-latents`.

Run the server:

```bash
cargo run -p comfyui-latents-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/comfyui-latents-app dev
```
