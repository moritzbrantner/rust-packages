# comfyui-latents-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `comfyui-latents-server`.

Run the server first:

```bash
cargo run -p comfyui-latents-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/comfyui-latents-app dev
```
