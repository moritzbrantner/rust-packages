# audio-analysis-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `audio-analysis-core`.

Run the server:

```bash
cargo run -p audio-analysis-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/audio-analysis-core-app dev
```
