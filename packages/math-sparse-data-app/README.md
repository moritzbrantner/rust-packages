# math-sparse-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `math-sparse-data-server`.

Run the server first:

```bash
cargo run -p math-sparse-data-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/math-sparse-data-app dev
```
