# video-analysis-models-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-models-server`.

Run the server first:

```bash
cargo run -p video-analysis-models-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/video-analysis-models-app dev
```
