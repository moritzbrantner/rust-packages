# three-d-scene-svg-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `three-d-scene-svg-server`.

Run the server first:

```bash
cargo run -p three-d-scene-svg-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/three-d-scene-svg-app dev
```
