# geo-io-osm-app

React, TypeScript, TailwindCSS, Bun, and Vite frontend for `geo-io-osm`.

```bash
cargo run -p geo-io-osm-server -- --addr 127.0.0.1:3000
bun run --cwd packages/geo-io-osm-app dev
```
