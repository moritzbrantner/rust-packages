# math-statistics-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `math-statistics-server`.

Run the server first:

```bash
cargo run -p math-statistics-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/math-statistics-app dev
```
