# video-analysis-onnx-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-onnx`.

Run the server:

```bash
cargo run -p video-analysis-onnx-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-onnx-app dev
```
