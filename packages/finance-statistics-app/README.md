# finance-statistics-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `finance-statistics`.

Run the server:

```bash
cargo run -p finance-statistics-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/finance-statistics-app dev
```
