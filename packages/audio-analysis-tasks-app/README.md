# audio-analysis-tasks-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `audio-analysis-tasks`.

Run the server:

```bash
cargo run -p audio-analysis-tasks-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/audio-analysis-tasks-app dev
```
