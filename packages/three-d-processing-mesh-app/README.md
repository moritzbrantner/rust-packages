# three-d-processing-mesh-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `three-d-processing-mesh`.

Run the server:

```bash
cargo run -p three-d-processing-mesh-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/three-d-processing-mesh-app dev
```
