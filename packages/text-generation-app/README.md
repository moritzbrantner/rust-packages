# text-generation-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `text-generation`.

Run the server:

```bash
cargo run -p text-generation-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/text-generation-app dev
```
