# video-analysis-opencv-backend-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-opencv-backend`.

Run the server:

```bash
cargo run -p video-analysis-opencv-backend-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-opencv-backend-app dev
```
