# model-runtime-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `model-runtime`.

Run the server:

```bash
cargo run -p model-runtime-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/model-runtime-app dev
```
