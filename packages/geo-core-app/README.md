# geo-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `geo-core`.

Run the server:

```bash
cargo run -p geo-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/geo-core-app dev
```
