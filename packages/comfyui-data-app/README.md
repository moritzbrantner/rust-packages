# comfyui-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `comfyui-data`.

Run the server:

```bash
cargo run -p comfyui-data-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/comfyui-data-app dev
```
