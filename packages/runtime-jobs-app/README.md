# runtime-jobs-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `runtime-jobs`.

Run the server:

```bash
cargo run -p runtime-jobs-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/runtime-jobs-app dev
```
