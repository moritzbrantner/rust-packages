# @moritzbrantner/text-analysis-wasm

WASM package for `text-analysis`.

Run the shared browser benchmark suite from the repository root:

```bash
bun run text-wasm:bench:all
```

Benchmark numbers are local to the current browser, machine, and build profile.
