# image-analysis-models-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `image-analysis-models`.

Run the server:

```bash
cargo run -p image-analysis-models-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/image-analysis-models-app dev
```
