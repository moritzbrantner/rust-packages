# video-analysis-radiance-pipeline-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-radiance-pipeline`.

Run the server:

```bash
cargo run -p video-analysis-radiance-pipeline-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-radiance-pipeline-app dev
```
