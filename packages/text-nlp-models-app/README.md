# text-nlp-models-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `text-nlp-models`.

Run the server:

```bash
cargo run -p text-nlp-models-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/text-nlp-models-app dev
```
