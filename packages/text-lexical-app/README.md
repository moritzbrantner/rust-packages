# text-lexical-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `text-lexical`.

Run the server:

```bash
cargo run -p text-lexical-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/text-lexical-app dev
```
