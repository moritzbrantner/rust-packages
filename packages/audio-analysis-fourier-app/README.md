# audio-analysis-fourier-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `audio-analysis-fourier`.

Run the server:

```bash
cargo run -p audio-analysis-fourier-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/audio-analysis-fourier-app dev
```
