# text-classification-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `text-classification`.

Run the server:

```bash
cargo run -p text-classification-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/text-classification-app dev
```
