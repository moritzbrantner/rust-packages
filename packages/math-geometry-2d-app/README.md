# math-geometry-2d-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `math-geometry-2d`.

Run the server:

```bash
cargo run -p math-geometry-2d-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/math-geometry-2d-app dev
```
