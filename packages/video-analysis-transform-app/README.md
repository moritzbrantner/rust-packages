# video-analysis-transform-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-transform-server`.

Run the server first:

```bash
cargo run -p video-analysis-transform-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/video-analysis-transform-app dev
```
