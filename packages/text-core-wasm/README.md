# @mb-rust/text-core-wasm

WASM bindings for the `text-core` Rust crate.

This package is intended to be consumed by higher-level TypeScript wrapper
packages rather than directly by applications.

Run `bun run bench:browser` from this package to rebuild the WASM bindings and
benchmark them in Chromium.
