# dense-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `dense-data-server`.

Run the server first:

```bash
cargo run -p dense-data-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/dense-data-app dev
```
