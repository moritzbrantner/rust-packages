# dense-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `dense-data`.

Run the server:

```bash
cargo run -p dense-data-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/dense-data-app dev
```
