# numbers-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `numbers-core`.

Run the server:

```bash
cargo run -p numbers-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/numbers-core-app dev
```
