# audio-analysis-speakers-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `audio-analysis-speakers`.

Run the server:

```bash
cargo run -p audio-analysis-speakers-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/audio-analysis-speakers-app dev
```
