# runtime-artifacts-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `runtime-artifacts`.

Run the server:

```bash
cargo run -p runtime-artifacts-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/runtime-artifacts-app dev
```
