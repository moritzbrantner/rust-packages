# video-analysis-sfm-rust-backend-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-sfm-rust-backend`.

Run the server:

```bash
cargo run -p video-analysis-sfm-rust-backend-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-sfm-rust-backend-app dev
```
