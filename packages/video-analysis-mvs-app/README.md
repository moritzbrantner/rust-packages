# video-analysis-mvs-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-mvs`.

Run the server:

```bash
cargo run -p video-analysis-mvs-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-mvs-app dev
```
