# finance-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `finance-data`.

Run the server:

```bash
cargo run -p finance-data-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/finance-data-app dev
```
