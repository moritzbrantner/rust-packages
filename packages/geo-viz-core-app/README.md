# geo-viz-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `geo-viz-core`.

Run the server:

```bash
cargo run -p geo-viz-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/geo-viz-core-app dev
```
