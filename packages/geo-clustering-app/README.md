# geo-clustering-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `geo-clustering`.

Run the server:

```bash
cargo run -p geo-clustering-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/geo-clustering-app dev
```
