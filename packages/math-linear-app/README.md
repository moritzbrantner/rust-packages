# math-linear-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `math-linear-server`.

Run the server first:

```bash
cargo run -p math-linear-server -- --addr 127.0.0.1:3000
```

Then run the app:

```bash
bun run --cwd packages/math-linear-app dev
```
