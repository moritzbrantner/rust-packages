# text-embeddings-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `text-embeddings`.

Run the server:

```bash
cargo run -p text-embeddings-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/text-embeddings-app dev
```
