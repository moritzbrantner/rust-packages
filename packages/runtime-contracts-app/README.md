# runtime-contracts-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `runtime-contracts`.

Run the server:

```bash
cargo run -p runtime-contracts-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/runtime-contracts-app dev
```
