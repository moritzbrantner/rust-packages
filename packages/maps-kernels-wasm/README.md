# @mb-rust/maps-kernels-wasm

WASM bindings for `maps-kernels-core`.

The package exposes flat `Float64Array` kernels for map and temporal GeoJSON
processing. JavaScript callers remain responsible for converting to and from
their domain objects.
