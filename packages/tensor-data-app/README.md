# tensor-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `tensor-data`.

Run the server:

```bash
cargo run -p tensor-data-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/tensor-data-app dev
```
