# jobs-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `jobs-core`.

Run the server:

```bash
cargo run -p jobs-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/jobs-core-app dev
```
