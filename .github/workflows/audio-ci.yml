name: audio-ci

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_BUILD_JOBS: "2"
  RUST_TEST_THREADS: "2"
  TEST_MAX_WORKERS: "2"

jobs:
  audio-fast:
    runs-on: ubuntu-latest
    steps:
      - name: Install Bun
        env:
          BUN_VERSION: 1.3.13
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) bun_target=bun-linux-x64 ;;
            aarch64|arm64) bun_target=bun-linux-aarch64 ;;
            *) echo "unsupported architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          curl -fsSL "https://github.com/oven-sh/bun/releases/download/bun-v${BUN_VERSION}/${bun_target}.zip" -o "$RUNNER_TEMP/bun.zip"
          unzip -q "$RUNNER_TEMP/bun.zip" -d "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/${bun_target}" >> "$GITHUB_PATH"
      - name: Checkout repository with Bun
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/bun-checkout.ts" <<'BUN'
          const workspace = process.env.GITHUB_WORKSPACE;
          const repository = process.env.GITHUB_REPOSITORY;
          const serverUrl = process.env.GITHUB_SERVER_URL ?? "https://github.com";
          const ref = process.env.GITHUB_REF;
          const sha = process.env.GITHUB_SHA;
          const token = process.env.GITHUB_TOKEN;

          if (!workspace || !repository || !ref) {
            throw new Error("GITHUB_WORKSPACE, GITHUB_REPOSITORY, and GITHUB_REF are required");
          }

          const remote = new URL(`${serverUrl.replace(/\/$/, "")}/${repository}.git`);
          if (remote.protocol === "https:" && token) {
            remote.username = "x-access-token";
            remote.password = token;
          }

          function git(...args: string[]) {
            const result = Bun.spawnSync(["git", ...args], {
              env: { ...process.env, GIT_TERMINAL_PROMPT: "0" },
              stdin: "inherit",
              stdout: "inherit",
              stderr: "inherit",
            });
            if (result.exitCode !== 0) {
              throw new Error(`git ${args.join(" ")} failed with status ${result.exitCode}`);
            }
          }

          git("init", workspace);
          git("-C", workspace, "config", "--global", "--add", "safe.directory", workspace);
          git("-C", workspace, "config", "--local", "gc.auto", "0");
          git("-C", workspace, "config", "--local", "advice.detachedHead", "false");
          Bun.spawnSync(["git", "-C", workspace, "remote", "remove", "origin"], { stdout: "ignore", stderr: "ignore" });
          git("-C", workspace, "remote", "add", "origin", remote.toString());
          git("-C", workspace, "fetch", "--force", "--no-tags", "--prune", "--no-recurse-submodules", "--depth=1", "origin", `+${ref}:refs/github-actions/checkout`);
          git("-C", workspace, "checkout", "--force", "--detach", sha ?? "refs/github-actions/checkout");
          BUN
          bun "$RUNNER_TEMP/bun-checkout.ts"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: >
          cargo clippy
          -p audio-analysis-core
          -p audio-analysis-fourier
          -p moritzbrantner-audio-analysis-io
          -p audio-analysis-pitch
          -p audio-analysis-processing
          -p audio-analysis-recognition
          -p audio-analysis-rhythm
          -p moritzbrantner-audio-analysis-separation
          --all-targets -- -D warnings
      - run: >
          PROPTEST_CASES=128 cargo test --jobs "$CARGO_BUILD_JOBS"
          -p audio-analysis-core
          -p audio-analysis-fourier
          -p moritzbrantner-audio-analysis-io
          -p audio-analysis-pitch
          -p audio-analysis-processing
          -p audio-analysis-recognition
          -p audio-analysis-rhythm
          -p moritzbrantner-audio-analysis-separation

  audio-ffmpeg:
    runs-on: ubuntu-latest
    steps:
      - name: Install Bun
        env:
          BUN_VERSION: 1.3.13
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) bun_target=bun-linux-x64 ;;
            aarch64|arm64) bun_target=bun-linux-aarch64 ;;
            *) echo "unsupported architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          curl -fsSL "https://github.com/oven-sh/bun/releases/download/bun-v${BUN_VERSION}/${bun_target}.zip" -o "$RUNNER_TEMP/bun.zip"
          unzip -q "$RUNNER_TEMP/bun.zip" -d "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/${bun_target}" >> "$GITHUB_PATH"
      - name: Checkout repository with Bun
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/bun-checkout.ts" <<'BUN'
          const workspace = process.env.GITHUB_WORKSPACE;
          const repository = process.env.GITHUB_REPOSITORY;
          const serverUrl = process.env.GITHUB_SERVER_URL ?? "https://github.com";
          const ref = process.env.GITHUB_REF;
          const sha = process.env.GITHUB_SHA;
          const token = process.env.GITHUB_TOKEN;

          if (!workspace || !repository || !ref) {
            throw new Error("GITHUB_WORKSPACE, GITHUB_REPOSITORY, and GITHUB_REF are required");
          }

          const remote = new URL(`${serverUrl.replace(/\/$/, "")}/${repository}.git`);
          if (remote.protocol === "https:" && token) {
            remote.username = "x-access-token";
            remote.password = token;
          }

          function git(...args: string[]) {
            const result = Bun.spawnSync(["git", ...args], {
              env: { ...process.env, GIT_TERMINAL_PROMPT: "0" },
              stdin: "inherit",
              stdout: "inherit",
              stderr: "inherit",
            });
            if (result.exitCode !== 0) {
              throw new Error(`git ${args.join(" ")} failed with status ${result.exitCode}`);
            }
          }

          git("init", workspace);
          git("-C", workspace, "config", "--global", "--add", "safe.directory", workspace);
          git("-C", workspace, "config", "--local", "gc.auto", "0");
          git("-C", workspace, "config", "--local", "advice.detachedHead", "false");
          Bun.spawnSync(["git", "-C", workspace, "remote", "remove", "origin"], { stdout: "ignore", stderr: "ignore" });
          git("-C", workspace, "remote", "add", "origin", remote.toString());
          git("-C", workspace, "fetch", "--force", "--no-tags", "--prune", "--no-recurse-submodules", "--depth=1", "origin", `+${ref}:refs/github-actions/checkout`);
          git("-C", workspace, "checkout", "--force", "--detach", sha ?? "refs/github-actions/checkout");
          BUN
          bun "$RUNNER_TEMP/bun-checkout.ts"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: bash scripts/setup_audio_external_tools.sh ffmpeg
      - run: FFMPEG_EXTERNAL_TESTS=1 cargo test --jobs "$CARGO_BUILD_JOBS" -p moritzbrantner-audio-analysis-io --test ffmpeg_decode

  audio-perf:
    runs-on: ubuntu-latest
    steps:
      - name: Install Bun
        env:
          BUN_VERSION: 1.3.13
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) bun_target=bun-linux-x64 ;;
            aarch64|arm64) bun_target=bun-linux-aarch64 ;;
            *) echo "unsupported architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          curl -fsSL "https://github.com/oven-sh/bun/releases/download/bun-v${BUN_VERSION}/${bun_target}.zip" -o "$RUNNER_TEMP/bun.zip"
          unzip -q "$RUNNER_TEMP/bun.zip" -d "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/${bun_target}" >> "$GITHUB_PATH"
      - name: Checkout repository with Bun
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/bun-checkout.ts" <<'BUN'
          const workspace = process.env.GITHUB_WORKSPACE;
          const repository = process.env.GITHUB_REPOSITORY;
          const serverUrl = process.env.GITHUB_SERVER_URL ?? "https://github.com";
          const ref = process.env.GITHUB_REF;
          const sha = process.env.GITHUB_SHA;
          const token = process.env.GITHUB_TOKEN;

          if (!workspace || !repository || !ref) {
            throw new Error("GITHUB_WORKSPACE, GITHUB_REPOSITORY, and GITHUB_REF are required");
          }

          const remote = new URL(`${serverUrl.replace(/\/$/, "")}/${repository}.git`);
          if (remote.protocol === "https:" && token) {
            remote.username = "x-access-token";
            remote.password = token;
          }

          function git(...args: string[]) {
            const result = Bun.spawnSync(["git", ...args], {
              env: { ...process.env, GIT_TERMINAL_PROMPT: "0" },
              stdin: "inherit",
              stdout: "inherit",
              stderr: "inherit",
            });
            if (result.exitCode !== 0) {
              throw new Error(`git ${args.join(" ")} failed with status ${result.exitCode}`);
            }
          }

          git("init", workspace);
          git("-C", workspace, "config", "--global", "--add", "safe.directory", workspace);
          git("-C", workspace, "config", "--local", "gc.auto", "0");
          git("-C", workspace, "config", "--local", "advice.detachedHead", "false");
          Bun.spawnSync(["git", "-C", workspace, "remote", "remove", "origin"], { stdout: "ignore", stderr: "ignore" });
          git("-C", workspace, "remote", "add", "origin", remote.toString());
          git("-C", workspace, "fetch", "--force", "--no-tags", "--prune", "--no-recurse-submodules", "--depth=1", "origin", `+${ref}:refs/github-actions/checkout`);
          git("-C", workspace, "checkout", "--force", "--detach", sha ?? "refs/github-actions/checkout");
          BUN
          bun "$RUNNER_TEMP/bun-checkout.ts"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: >
          cargo bench --jobs "$CARGO_BUILD_JOBS"
          -p audio-analysis-core
          -p audio-analysis-fourier
          -p audio-analysis-pitch
          -p audio-analysis-processing
          -p audio-analysis-recognition
          -p audio-analysis-rhythm
      - run: python3 scripts/check_audio_bench.py

  audio-demucs-smoke:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Install Bun
        env:
          BUN_VERSION: 1.3.13
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) bun_target=bun-linux-x64 ;;
            aarch64|arm64) bun_target=bun-linux-aarch64 ;;
            *) echo "unsupported architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          curl -fsSL "https://github.com/oven-sh/bun/releases/download/bun-v${BUN_VERSION}/${bun_target}.zip" -o "$RUNNER_TEMP/bun.zip"
          unzip -q "$RUNNER_TEMP/bun.zip" -d "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/${bun_target}" >> "$GITHUB_PATH"
      - name: Checkout repository with Bun
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/bun-checkout.ts" <<'BUN'
          const workspace = process.env.GITHUB_WORKSPACE;
          const repository = process.env.GITHUB_REPOSITORY;
          const serverUrl = process.env.GITHUB_SERVER_URL ?? "https://github.com";
          const ref = process.env.GITHUB_REF;
          const sha = process.env.GITHUB_SHA;
          const token = process.env.GITHUB_TOKEN;

          if (!workspace || !repository || !ref) {
            throw new Error("GITHUB_WORKSPACE, GITHUB_REPOSITORY, and GITHUB_REF are required");
          }

          const remote = new URL(`${serverUrl.replace(/\/$/, "")}/${repository}.git`);
          if (remote.protocol === "https:" && token) {
            remote.username = "x-access-token";
            remote.password = token;
          }

          function git(...args: string[]) {
            const result = Bun.spawnSync(["git", ...args], {
              env: { ...process.env, GIT_TERMINAL_PROMPT: "0" },
              stdin: "inherit",
              stdout: "inherit",
              stderr: "inherit",
            });
            if (result.exitCode !== 0) {
              throw new Error(`git ${args.join(" ")} failed with status ${result.exitCode}`);
            }
          }

          git("init", workspace);
          git("-C", workspace, "config", "--global", "--add", "safe.directory", workspace);
          git("-C", workspace, "config", "--local", "gc.auto", "0");
          git("-C", workspace, "config", "--local", "advice.detachedHead", "false");
          Bun.spawnSync(["git", "-C", workspace, "remote", "remove", "origin"], { stdout: "ignore", stderr: "ignore" });
          git("-C", workspace, "remote", "add", "origin", remote.toString());
          git("-C", workspace, "fetch", "--force", "--no-tags", "--prune", "--no-recurse-submodules", "--depth=1", "origin", `+${ref}:refs/github-actions/checkout`);
          git("-C", workspace, "checkout", "--force", "--detach", sha ?? "refs/github-actions/checkout");
          BUN
          bun "$RUNNER_TEMP/bun-checkout.ts"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: bash scripts/setup_audio_external_tools.sh conda demucs
        env:
          AUDIO_DEMUCS_INSTALLER: auto
      - run: >
          RUN_REAL_DEMUCS_TESTS=1 cargo test --jobs "$CARGO_BUILD_JOBS" -p moritzbrantner-audio-analysis-separation
          real_demucs_smoke_test_when_requested -- --ignored --nocapture
