name: full-workspace-ci

on:
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  full-workspace:
    uses: moritzbrantner/reusable-workflows/.github/workflows/e2e-validation.yml@workflow-standard-v1
    with:
      bun_version: "1.3.14"
      node_version: ""
      install_command: bun install --frozen-lockfile
      pre_command: rustup toolchain install 1.95.0 --profile minimal --component clippy --component rustfmt && rustup default 1.95.0 && rustup target add wasm32-unknown-unknown && cargo install wasm-pack --locked --version 0.14.0
      build_command: BASE_REF=HEAD scripts/check-preflight.sh
      e2e_command: bun run --cwd packages/video-analysis-ui storybook:test:ci
      install_playwright: true
      cache_cargo: true
      timeout_minutes: 120
