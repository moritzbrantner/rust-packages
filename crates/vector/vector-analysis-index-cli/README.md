# vector-analysis-index-cli

Thin command-line adapter for `vector-analysis-index`.

Run:

```bash
cargo run -p vector-analysis-index-cli -- operations --json
cargo run -p vector-analysis-index-cli -- run --operation describe --json '{"includeOperations":true}'
```
