# model-runtime-cli

Thin command-line adapter for `moritzbrantner-model-runtime`.

Run:

```bash
cargo run -p model-runtime-cli -- operations --json
cargo run -p model-runtime-cli -- run --operation describe --json '{"includeOperations":true}'
```
