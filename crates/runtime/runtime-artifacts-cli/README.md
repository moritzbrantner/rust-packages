# runtime-artifacts-cli

Thin command-line adapter for `runtime-artifacts`.

Run:

```bash
cargo run -p runtime-artifacts-cli -- operations --json
cargo run -p runtime-artifacts-cli -- run --operation describe --json '{"includeOperations":true}'
```
