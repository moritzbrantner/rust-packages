# runtime-jobs-cli

Thin command-line adapter for `runtime-jobs`.

Run:

```bash
cargo run -p runtime-jobs-cli -- operations --json
cargo run -p runtime-jobs-cli -- run --operation describe --json '{"includeOperations":true}'
```
