# runtime-artifacts

Shared artifact references and small artifact stores for operation outputs.

The first local layout is:

```text
.workflow-output/
  jobs/
    <job-id>/
      manifest.json
      artifacts/
        ...
      logs/
        run.log
```
