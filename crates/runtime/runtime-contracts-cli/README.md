# runtime-contracts-cli

Thin command-line adapter for `runtime-contracts`.

Run:

```bash
cargo run -p runtime-contracts-cli -- operations --json
cargo run -p runtime-contracts-cli -- run --operation describe --json '{"includeOperations":true}'
```
