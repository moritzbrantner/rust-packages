# jobs-core-cli

Thin command-line adapter for `jobs-core`.

Run:

```bash
cargo run -p jobs-core-cli -- operations --json
cargo run -p jobs-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
