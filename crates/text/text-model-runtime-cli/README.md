# text-model-runtime-cli

Thin command-line adapter for `moritzbrantner-text-model-runtime`.

Run:

```bash
cargo run -p text-model-runtime-cli -- operations --json
cargo run -p text-model-runtime-cli -- run --operation describe --json '{"includeOperations":true}'
```
