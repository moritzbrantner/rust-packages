# text-classification-cli

Thin command-line adapter for `moritzbrantner-text-classification`.

Run:

```bash
cargo run -p text-classification-cli -- operations --json
cargo run -p text-classification-cli -- run --operation describe --json '{"includeOperations":true}'
```
