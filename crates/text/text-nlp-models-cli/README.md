# text-nlp-models-cli

Thin command-line adapter for `text-nlp-models`.

Run:

```bash
cargo run -p text-nlp-models-cli -- operations --json
cargo run -p text-nlp-models-cli -- run --operation describe --json '{"includeOperations":true}'
```
