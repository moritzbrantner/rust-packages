# text-question-answering-cli

Thin command-line adapter for `moritzbrantner-text-question-answering`.

Run:

```bash
cargo run -p text-question-answering-cli -- operations --json
cargo run -p text-question-answering-cli -- run --operation describe --json '{"includeOperations":true}'
```
