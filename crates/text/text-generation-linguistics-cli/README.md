# text-generation-linguistics-cli

Thin command-line adapter for `moritzbrantner-text-generation-linguistics`.

Run:

```bash
cargo run -p text-generation-linguistics-cli -- operations --json
cargo run -p text-generation-linguistics-cli -- run --operation describe --json '{"includeOperations":true}'
```
