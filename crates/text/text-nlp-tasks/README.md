# text-nlp-tasks

Shared request/response schemas, task catalogs, fallback runners, and
imported-prediction postprocessing for text NLP tasks.

`text-nlp-models` remains available as a compatibility crate for one migration
window, but new code should depend on `text-nlp-tasks`.
