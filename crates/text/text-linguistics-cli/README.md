# text-linguistics-cli

Thin command-line adapter for `text-linguistics`.

Run:

```bash
cargo run -p text-linguistics-cli -- operations --json
cargo run -p text-linguistics-cli -- run --operation describe --json '{"includeOperations":true}'
```
