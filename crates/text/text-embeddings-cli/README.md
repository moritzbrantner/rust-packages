# text-embeddings-cli

Thin command-line adapter for `moritzbrantner-text-embeddings`.

Run:

```bash
cargo run -p text-embeddings-cli -- operations --json
cargo run -p text-embeddings-cli -- run --operation describe --json '{"includeOperations":true}'
```
