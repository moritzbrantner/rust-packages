# text-nlp-cli

Aggregate CLI for shared text NLP tasks.

Examples:

```bash
cargo run -p text-nlp-cli -- sentiment --text "excellent reliable work" --fallback lexical
cargo run -p text-nlp-cli -- summarize --text "First sentence. Second sentence."
```

