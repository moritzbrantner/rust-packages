# text-generation-cli

Thin command-line adapter for `moritzbrantner-text-generation`.

Run:

```bash
cargo run -p text-generation-cli -- operations --json
cargo run -p text-generation-cli -- run --operation describe --json '{"includeOperations":true}'
```
