# text-lexical-cli

Thin command-line adapter for `text-lexical`.

Run:

```bash
cargo run -p text-lexical-cli -- operations --json
cargo run -p text-lexical-cli -- run --operation describe --json '{"includeOperations":true}'
```
