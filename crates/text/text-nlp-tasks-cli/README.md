# text-nlp-tasks-cli

Thin command-line adapter for `text-nlp-tasks`.

Run:

```bash
cargo run -p text-nlp-tasks-cli -- operations --json
cargo run -p text-nlp-tasks-cli -- run --operation describe --json '{"includeOperations":true}'
```
