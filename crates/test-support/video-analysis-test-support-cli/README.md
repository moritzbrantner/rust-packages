# video-analysis-test-support-cli

Thin command-line adapter for `moritzbrantner-video-analysis-test-support`.

Run:

```bash
cargo run -p video-analysis-test-support-cli -- operations --json
cargo run -p video-analysis-test-support-cli -- run --operation describe --json '{"includeOperations":true}'
```
