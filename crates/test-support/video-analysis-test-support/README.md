# video-analysis-test-support

Small fixture builders and environment helpers for workspace tests.

## Runtime Surface

- `testSupport.catalog` lists helper fixture categories.
- `testSupport.minimalReport` summarizes small scene, text, and dataset
  contract fixtures without producing complex video or 3D payloads.
