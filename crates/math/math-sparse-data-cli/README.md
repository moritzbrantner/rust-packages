# math-sparse-data-cli

Thin command-line adapter for `moritzbrantner-math-sparse-data`.

Run:

```bash
cargo run -p math-sparse-data-cli -- operations --json
cargo run -p math-sparse-data-cli -- run --operation describe --json '{"includeOperations":true}'
```
