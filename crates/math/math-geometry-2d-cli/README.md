# math-geometry-2d-cli

Thin command-line adapter for `math-geometry-2d`.

Run:

```bash
cargo run -p math-geometry-2d-cli -- operations --json
cargo run -p math-geometry-2d-cli -- run --operation describe --json '{"includeOperations":true}'
```
