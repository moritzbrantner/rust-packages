# math-signal-core-cli

Thin command-line adapter for `math-signal-core`.

Run:

```bash
cargo run -p math-signal-core-cli -- operations --json
cargo run -p math-signal-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
