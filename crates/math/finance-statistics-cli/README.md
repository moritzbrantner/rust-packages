# finance-statistics-cli

Thin command-line adapter for `finance-statistics`.

Run:

```bash
cargo run -p finance-statistics-cli -- operations --json
cargo run -p finance-statistics-cli -- run --operation describe --json '{"includeOperations":true}'
```
