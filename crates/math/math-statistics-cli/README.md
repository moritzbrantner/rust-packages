# math-statistics-cli

Thin command-line adapter for `moritzbrantner-math-statistics`.

Run:

```bash
cargo run -p math-statistics-cli -- operations --json
cargo run -p math-statistics-cli -- run --operation describe --json '{"includeOperations":true}'
```
