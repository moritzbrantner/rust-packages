# geo-io-geojson-cli

Thin command-line adapter for `geo-io-geojson`.

Run:

```bash
cargo run -p geo-io-geojson-cli -- operations --json
cargo run -p geo-io-geojson-cli -- run --operation describe --json '{"includeOperations":true}'
```
