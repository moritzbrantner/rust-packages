# geo-data-cli

Thin command-line adapter for `geo-data`.

Run:

```bash
cargo run -p geo-data-cli -- operations --json
cargo run -p geo-data-cli -- run --operation describe --json '{"includeOperations":true}'
```
