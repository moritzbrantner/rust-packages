# data-inversion-core-cli

Thin command-line adapter for `data-inversion-core`.

Run:

```bash
cargo run -p data-inversion-core-cli -- operations --json
cargo run -p data-inversion-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
