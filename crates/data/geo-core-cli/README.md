# geo-core-cli

Thin command-line adapter for `geo-core`.

Run:

```bash
cargo run -p geo-core-cli -- operations --json
cargo run -p geo-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
