# dense-data-cli

Thin command-line adapter for `dense-data`.

Run:

```bash
cargo run -p dense-data-cli -- operations --json
cargo run -p dense-data-cli -- run --operation describe --json '{"includeOperations":true}'
```
