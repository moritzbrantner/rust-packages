# geo-clustering-cli

Thin command-line adapter for `geo-clustering`.

Run:

```bash
cargo run -p geo-clustering-cli -- operations --json
cargo run -p geo-clustering-cli -- run --operation describe --json '{"includeOperations":true}'
```
