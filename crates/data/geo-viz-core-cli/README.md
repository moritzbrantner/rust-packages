# geo-viz-core-cli

Thin command-line adapter for `geo-viz-core`.

Run:

```bash
cargo run -p geo-viz-core-cli -- operations --json
cargo run -p geo-viz-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
