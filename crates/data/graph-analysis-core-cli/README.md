# graph-analysis-core-cli

Thin command-line adapter for `graph-analysis-core`.

Run:

```bash
cargo run -p graph-analysis-core-cli -- operations --json
cargo run -p graph-analysis-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
