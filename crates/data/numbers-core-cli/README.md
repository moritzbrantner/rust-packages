# numbers-core-cli

Thin command-line adapter for `numbers-core`.

Run:

```bash
cargo run -p numbers-core-cli -- operations --json
cargo run -p numbers-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
