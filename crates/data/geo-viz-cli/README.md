# geo-viz-cli

Thin command-line adapter for `geo-viz`.

Run:

```bash
cargo run -p geo-viz-cli -- operations --json
cargo run -p geo-viz-cli -- run --operation describe --json '{"includeOperations":true}'
```
