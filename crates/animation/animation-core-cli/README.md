# animation-core-cli

Thin command-line adapter for `moritzbrantner-animation-core`.

Run:

```bash
cargo run -p animation-core-cli -- operations --json
cargo run -p animation-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
