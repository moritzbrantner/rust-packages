# image-analysis-tasks-cli

Thin command-line adapter for `image-analysis-tasks`.

Run:

```bash
cargo run -p image-analysis-tasks-cli -- operations --json
cargo run -p image-analysis-tasks-cli -- run --operation describe --json '{"includeOperations":true}'
```
