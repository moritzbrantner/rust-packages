# image-analysis-onnx-cli

Thin command-line adapter for `moritzbrantner-image-analysis-onnx`.

Run:

```bash
cargo run -p image-analysis-onnx-cli -- operations --json
cargo run -p image-analysis-onnx-cli -- run --operation describe --json '{"includeOperations":true}'
```
