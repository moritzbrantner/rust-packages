# audio-analysis-test-support-cli

Thin command-line adapter for `moritzbrantner-audio-analysis-test-support`.

Run:

```bash
cargo run -p audio-analysis-test-support-cli -- operations --json
cargo run -p audio-analysis-test-support-cli -- run --operation describe --json '{"includeOperations":true}'
```
