# audio-analysis-core-cli

Thin command-line adapter for `audio-analysis-core`.

Run:

```bash
cargo run -p audio-analysis-core-cli -- operations --json
cargo run -p audio-analysis-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
