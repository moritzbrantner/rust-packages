# audio-analysis-io-cli

Thin command-line adapter for `moritzbrantner-audio-analysis-io`.

Run:

```bash
cargo run -p audio-analysis-io-cli -- operations --json
cargo run -p audio-analysis-io-cli -- run --operation describe --json '{"includeOperations":true}'
```
