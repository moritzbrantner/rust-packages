# audio-analysis-tasks-cli

Thin command-line adapter for `audio-analysis-tasks`.

Run:

```bash
cargo run -p audio-analysis-tasks-cli -- operations --json
cargo run -p audio-analysis-tasks-cli -- run --operation describe --json '{"includeOperations":true}'
```
