# audio-generation-midi-cli

Thin command-line adapter for `audio-generation-midi`.

Run:

```bash
cargo run -p audio-generation-midi-cli -- operations --json
cargo run -p audio-generation-midi-cli -- run --operation describe --json '{"includeOperations":true}'
```
