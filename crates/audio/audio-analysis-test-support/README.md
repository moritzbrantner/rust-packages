# audio-analysis-test-support

Shared deterministic fixtures and helpers for audio crate tests and package
surface checks.

The crate generates in-memory sine waves, impulse trains, click tracks, noise,
and owned audio frames without downloading models or invoking external tools.
It is test support only and is not published.
