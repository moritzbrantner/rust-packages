# audio-analysis-speakers-cli

Thin command-line adapter for `moritzbrantner-audio-analysis-speakers`.

Run:

```bash
cargo run -p audio-analysis-speakers-cli -- operations --json
cargo run -p audio-analysis-speakers-cli -- run --operation describe --json '{"includeOperations":true}'
```
