# video-analysis-posture-io-cli

Thin command-line adapter for `moritzbrantner-video-analysis-posture-io`.

Run:

```bash
cargo run -p video-analysis-posture-io-cli -- operations --json
cargo run -p video-analysis-posture-io-cli -- run --operation describe --json '{"includeOperations":true}'
```
