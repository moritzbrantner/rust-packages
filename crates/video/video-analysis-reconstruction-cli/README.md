# video-analysis-reconstruction-cli

Thin command-line adapter for `video-analysis-reconstruction`.

Run:

```bash
cargo run -p video-analysis-reconstruction-cli -- operations --json
cargo run -p video-analysis-reconstruction-cli -- run --operation describe --json '{"includeOperations":true}'
```
