# video-analysis-radiance-io-cli

Thin command-line adapter for `video-analysis-radiance-io`.

Run:

```bash
cargo run -p video-analysis-radiance-io-cli -- operations --json
cargo run -p video-analysis-radiance-io-cli -- run --operation describe --json '{"includeOperations":true}'
```
