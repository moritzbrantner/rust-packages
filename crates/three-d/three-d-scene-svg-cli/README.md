# three-d-scene-svg-cli

Thin command-line adapter for `three-d-scene-svg`.

Run:

```bash
cargo run -p three-d-scene-svg-cli -- operations --json
cargo run -p three-d-scene-svg-cli -- run --operation describe --json '{"includeOperations":true}'
```
